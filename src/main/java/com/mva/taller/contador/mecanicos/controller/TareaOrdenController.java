//package com.mva.taller.contador.mecanicos.controller;
import com.mva.taller.contador.mecanicos.dao.ITareaOrdenDao;
/*import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;

@Controller
public class TareaOrdenController {

    @Autowired
    private ITareaOrdenDao tareaOrdenDao;

    @RequestMapping(value="/listar", method= RequestMethod.GET)
    public String listar(Model model){
        model.addAttribute("titulo","Listado de Orden de Tareas");
        model.addAttribute("tareaorden",tareaOrdenDao.findAll());
        return "listar";
    }
}*/
